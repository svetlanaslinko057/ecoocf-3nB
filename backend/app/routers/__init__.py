"""FastAPI APIRouter modules.

Each file exposes a single ``router`` object that ``server.py`` will
``include_router(...)`` during P1 of the refactoring.

Ownership convention: each router file starts with a header comment
listing the domain owner and the extraction date.
"""
