"""Wave 2A — Calls Foundation

Provides aggregated call history per customer (read-only) +
recording proxy with Range support. No new AI generation here.

Mounted via ``server.py`` -> ``include_router(prefix='/api')``.
"""
